# Agent Notes

Prefer failing loudly with clear error logs over failing silently with hidden fallbacks.

The role of this file is to describe common mistakes and confusion points that agents may encounter as they work in this project.

If you ever encounter something in the project that surprises you, please alert the developer working with you to indicate that this is the case in the AgentMD file to help prevent future agents from having the same issue.

## Working Rule

- Refer to this file at the start of each task and update it when new confusion points are discovered.

## Project Rules

- This is a robot control project. Prefer small, reviewable changes.
- Do not change behavior during refactor-only tasks.
- Do not modify `current_sensing.py`, gripper safety, current limits, or arm safety unless explicitly requested.
- Before adding a new helper, search for existing similar logic.
- Do not duplicate YOLO wrappers, candidate filtering, UV/depth projection, track memory, stack identity, section inference, `pick_misplaced`, or return/drop-zone logic.
- Track IDs are not physical cube identity. For stacked cubes, use geometry, XYZ, section, layer, color, and confidence.
- Every implementation summary must include:
  1. files changed
  2. behavior changed or not changed
  3. existing helpers checked
  4. validation performed
  5. risks

## Project Notes

- Recenter/verify drift can come from detection model instability; prefer track-id continuity whenever possible.
- Exception: for `pick_placed_left/right`, top-cube intent must remain explicit so correction picks do not target lower cubes.
- Pick-placed handoff/verify split (2026-05-27): `pick_placed_left/right` handoff ranks by **target image half**, then **max projected Z** when XYZ is available (`QARM_PICK_PLACED_HANDOFF_PREFER_MAX_Z=true`, log `rank=max_z`), else **top `v`** (`rank=pixel_top`); do not use projected XYZ for side at handoff when `QARM_PICK_PLACED_HANDOFF_SECTION_HARD_FILTER=false` (default). Accept/reject uses **post-center `median_xyz` only** (`[PickMisplacedLockVerify]`). Height gate (`QARM_MISPLACED_PICK_ALLOW_HIGHER_THAN_EXPECTED=true`, default): accept measured level **above** authoritative expected (hydrate under-count, `ok_higher_than_expected` + `raise_state_to_measured`); reject **below** expected; exact match still uses Z tol. After successful correction drop, `_reconcile_authoritative_stack_level_to_measured` then `pop_authoritative_stack_top` updates planner level. Rollback: `QARM_MISPLACED_PICK_ALLOW_HIGHER_THAN_EXPECTED=false`, `QARM_PICK_PLACED_HANDOFF_PREFER_MAX_Z=false`. Wrong section → terminal reject in one strike (`QARM_PICK_PLACED_VERIFY_STRIKES=1`). `pick_misplaced_*` keeps 2-strike probe and verify pixel fallback on `too_far_from_section_center_xy`. Rollback handoff filter: `QARM_PICK_PLACED_HANDOFF_SECTION_HARD_FILTER=true`.
- Keep planner state lean. Avoid re-introducing duplicated or competing state sources that can confuse action selection.
- In this robotics project, explicit stops/freezes are preferred over hidden automatic fallback behavior.
- Avoid adding fallbacks whenever possible; default to explicit failure with clear logs.
- If a fallback is truly necessary for safety, it must be transparent in logs, easy to reason about, and narrowly scoped.
- Stack-anchor caution: re-anchoring XY to the latest top-cube verify measurement can create cumulative "staircase" drift; prefer a fixed base anchor per side during stack build.
- Startup hydrate note: `QARM_STARTUP_STACK_LOCK_FRAMES` must be wired into the lock session; if not, startup lock silently uses global `CENTERED_FRAMES_REQUIRED` and can make per-track rounds look stuck despite tuned startup config.
- Capacity note: setting `QARM_MAX_STACK_LEVELS_PER_SECTION` below 3 can prematurely force "full stack capacity" behavior (e.g., stopping at 4 cubes total); keep effective max at 3 for standard 3-high missions.
- Startup hydrate track-ID note: discovery groups currently preserve the first assigned `track_id`; if ByteTrack remaps the same physical cube later, the group can keep a stale ID and startup lock may wait on a non-visible required track.
- Startup hydrate control-flow note: if startup target processing iterates a frozen list snapshot, newly visible `track_id`s are never attempted; use an index-based loop so list growth is actually processed.
- Status update: startup hydrate is now working well with live track-id updates and visible-target processing.
- Startup hydrate config note: after reverting to the older hydrate flow, remove newer-only/unused startup knobs first before tuning behavior so logs and active control flow stay aligned.
- Color-classify policy note: for lock/centered UV color reads, use the detection bbox for the active/locked `track_id` (nearest to lock UV), then core-patch color; avoid non-track UV-only bbox fallback that can mix neighboring cubes.
- Next issue focus: `pick_misplaced_left/right` should enforce top-targeting and actively sort detections into left/right during centering so correction picks target the correct side/top cube.
- Stack anchor note: runtime column XY is sticky **commanded** `expected_xyz` from the first successful level-0 place (`commanded_place_base_level0`); upper-layer verify `measured_xyz` must not move anchor. `get_latest_side_stack_anchor_xyz` priority: (1) locked commanded base, (2) ledger level-0 `expected_xyz`, (3) `startup_hydrate_top` (measured hydrate only), (4) `last_popped_xy`. Commit on `place_ok` at level 0 via `[StackAnchorCommit]`. Missing anchor logs `[StackAnchor] missing ... commanded_level0=...`.
- Stack anchor place_context note: stack-mode (`place_left_stack` / `place_right_stack`) `place_context.stack_level` must be pre-place `level` (same as `place_left` / `place_right`), not `level+1`; `pending_stack_level` stays `level+1`. Using `level+1` for `stack_level` skips `[StackAnchorCommit]` on the first stack place and causes `stack_anchor_missing_side_xyz` on the second stack layer.
- Stack pick-X offset note (2026-05-29): fixed stack-level command X offsets are disabled by default (`QARM_STACK_X_LEVEL_OFFSET_ENABLED=false`). `place_left_stack` / `place_right_stack` now apply command-only X offset from the successful grasp's latched measured pick X (`state.last_pick_measured_xyz`, `QARM_STACK_PICK_X_OFFSET_ENABLED=true`; log `[StackPickXOffset]`) only when stacking above base (`level > 0`) and stack anchor source is `commanded_place_base_level0`; skip for hydrate/verify/measured anchors (`startup_hydrate_top`, `last_popped_xy`, etc.) with `reason=anchor_source_no_pick_x_offset:<source>`. Defaults are tuned from centered pick-area measurements: near pick X 0.320 m -> -0.006 m and far pick X 0.526 m -> +0.010 m, clamped -0.005..+0.010. Verification/ledger `expected_xyz` and stack anchors must remain the nominal unshifted target. Do not apply this to base `place_left` / `place_right`, Y, Z, pick motion, or verify targets.
- Prompt 2 stack observation (2026-05-29): while gathering successful runs with minimal orientation changes, the second orange cube stacked for Prompt 2 tends to land slightly forward. Do not tune yet; keep collecting results before changing placement/orientation offsets.
- Policy I/O (prompted mode, default v21): Operator file `llm_commander/prompts/live_sort_operator_v21.txt` via `QARM_LLM_POLICY_PROMPT_PATH` (default in `runtime_core.DEFAULT_POLICY_PROMPT_PATH`). Mission text: `QARM_MISSION_PROMPT` (also in state). Each step Ollama receives `{prompt_text}\n\nINPUT_JSON:\n{...}` built by `LivePolicyBrain.decide` (`planner_io.build_prompted_planner_state` + `allowed_commands` from `runtime_loop_cycle`). Trace: `QARM_POLICY_TRACE_SAVE_RAW` → `policy_raw_*.jsonl` with full `llm_input`.
- `INPUT_JSON` top level: `allowed_commands` (string list, legality only), `state` (object below). On reprompt only: optional `last_error`.
- `state` fields shipped to LLM: `mission_prompt`, `phase` (`observe`|`classification`|`grasp`|`place`), `holding_object`, `scene_empty_confirmed`, `held_cube` OR `pick_target` (mutually exclusive by holding), `section_status.left` / `section_status.right`. Not shipped: `top_color`, `section_status_source`, `command_context`, `mission_spec`.
- `held_cube` when holding: `{color: blue|orange|unknown, conf: 0-1}`. `pick_target` when not holding: `{color: blue|orange|unknown, conf: 0-1, is_centered: bool}`.
- `section_status.*` per side (authoritative stack from hydrate/placement updates): `{stack_level: 0-3, slots: {base, middle, top}}` where slot values are `blue`|`orange`|`unknown`|`empty` (`empty` = unfilled above current height). Optional if `QARM_PLANNER_INCLUDE_COLOR_SEQUENCE=true`: `color_sequence_bottom_to_top`.
- v21 behavior: v20 generalized further by removing the redundant JSON-key rule, removing Prompt-3-specific hardcoded `[BLUE, ORANGE, BLUE]` stop wording, keeping held-cube placement side/color derived only from `mission_prompt`, and aligning `pick_other` wording with current `pick_target` state (classify only if unknown; grasp if known and mission-legal). Rollback prompt: `QARM_LLM_POLICY_PROMPT_PATH` → v20, v19.1, v19, v18, or v17.
- Policy tuning: `QARM_LLM_POLICY_NUM_PREDICT` (default 0 = no cap). Console `[PlannerStack]` mirrors `slots`/`stack_level` (no `section_status_source` in LLM payload).
- Stack-state log caution (2026-05-30): `[StackState]` post-place logging now preserves `unknown` values in `color_sequence_bottom_to_top`; if older logs appear to shift colors between layers, compare against `[PlannerStack]`, `authoritative_place_update.sequence`, or the raw hydrated row.
- Place Y bias / grasp stack-forward X (2026-05): `QARM_PLACE_Y_BIAS_M` default **0** (was 0.006 on slot place). `QARM_GRASP_STACK_FORWARD_ENABLE` default **false**, `QARM_GRASP_STACK_FORWARD_PER_LEVEL_M=0` (was +1.2 mm/level from Z). Re-enable via env after stack placement tune.
- Place grid Y spacing (2026-05-29): defaults are `QARM_PLACE_GRID_CENTER_Y_M=0.1625` and `QARM_PLACE_GRID_DY_M=0.0625`, placing right y=0.100 and left y=0.225. This moves the left stack inward to reduce reach angle while keeping section separation at 0.125 m (> `QARM_PLACE_MIN_SEP_M=0.075`). Previous configs: dy 0.075 around 0.225 (right 0.150, left 0.300), then dy 0.060 around 0.225 (right 0.165, left 0.285).
- Place pick-bias compensate (2026-05): all place commands subtract scaled grasp pick bias from XY (`QARM_PLACE_PICK_BIAS_COMPENSATE_ENABLED=true`, `QARM_PLACE_PICK_BIAS_COMPENSATE_SCALE=1.0`) — inverse of `GRASP_PICK_X_BIAS_M` / `GRASP_PICK_Y_BIAS_M` at release; Z unchanged. Log `[PlacePickBiasComp]`. Tune partial: `SCALE=0.5`. Rollback: `QARM_PLACE_PICK_BIAS_COMPENSATE_ENABLED=false`.
- Measurement / place XY policy (2026-05): **(1)** Every projected cube gets scan correction in `projection_geometry.apply_scan_base_xy_offset` when `QARM_SCAN_BASE_XY_OFFSET_ENABLED=true` (default): `QARM_SCAN_BASE_X_OFFSET_M=+0.007`, `QARM_SCAN_BASE_Y_OFFSET_M=+0.021` (lifts raw projection toward command frame; **Z never shifted**). Hydrate, tracks, pick seeds, and verify share corrected XYZ. **(2)** Place commands use anchor/slot XY as-is (`QARM_PLACE_CMD_XY_OFFSET_ENABLED=false`). **(3)** Verify eval offsets (`QARM_PLACE_VERIFY_V2_EXPECTED_EVAL_USE_OFFSETS`, default **false**) are optional fine-tuning on `expected` only — do not enable together with full scan Y until A/B tuned or Y is double-corrected. Tunable eval when scan off: `QARM_PLACE_VERIFY_V2_EXPECTED_EVAL_USE_OFFSETS=true`, `y_ref_offset=-0.021`. Rollback scan: `QARM_SCAN_BASE_XY_OFFSET_ENABLED=false`.
- Verify-v2 reject note: for `placed_mismatch_out_of_margin`, avoid broad XYZ blacklist side effects; if nearby top-cube candidates are close, XYZ blocking can suppress the truly correct placement candidate after one wrong-track reject.
- Verify-v2 targeting note (2026-05-29): post-place verify tries expected-slot scan + `center_object_on_expected_slot(..., target_mode_override="filtered_first")`, then restores the older track-lock measurement stage (`run_track_handoff_session` + `_verify_on_locked_candidate`) so a visible/centered cube is measured before startup hydrate runs. Do not re-add extra in-verify association scans such as `slot_scan_association`; the explicit, logged startup-hydrate refresh in `runtime_loop_actions_place.py` remains the final recovery after unconfirmed verify. Do not let wrong-target color samples update `cube_color` unless geometry is confirmed or inside margins.
- Verify-v2 retry/fallback note (2026-05-29): post-place verify now runs the expected-slot recenter ladder with one retry (`QARM_PLACE_VERIFY_V2_EXPECTED_SLOT_RETRIES=1`) and then checks the top target-side track candidates (`QARM_PLACE_VERIFY_V2_TOP_CANDIDATE_CHECKS=2`) before deferring generic handoff. Startup-hydrate fallback is enabled by default (`QARM_PLACE_VERIFY_V2_HYDRATE_FALLBACK_ENABLED=true`) and generic handoff is deferred to it for pending stack placements (`QARM_PLACE_VERIFY_V2_DEFER_GENERIC_HANDOFF_TO_HYDRATE=true`) so verify does not quickly reject based on a wrong-side generic track. New verbose ladder logs (`[PlaceVerifyRetry]`, `[PlaceVerifyTop2]`, `[PlaceVerifyGenericHandoff]`) are controlled by `QARM_PLACE_VERIFY_V2_LADDER_LOGS` (default true while tuning; set false once stable).
- Verify-v2 pre-lock projection note (2026-05-29): post-place verify recenter must not reject or blacklist candidates from projected XYZ before centering. Verify recenter disables pre-lock projected-XYZ filtering, wrong-XY pixel blacklists, and projected section filtering; use post-lock geometry/slot scan to accept or reject.
- Verify-v2 negative-Y avoidance note (2026-05-29): verify recenter and verify track handoff skip candidates with projected base Y below `QARM_PLACE_VERIFY_V2_MIN_TRACK_Y_M=-0.020` when `QARM_PLACE_VERIFY_V2_AVOID_NEGATIVE_Y=true` (default). This avoids centering on clearly negative-Y pick-space cubes while still allowing low positive-Y right-stack candidates.
- Verify-v2 active-centering timer note (2026-05-29): verify recenter calls `center_object_on_expected_slot(..., reset_timeout_on_first_candidate=True)` so the search window and active centering window are separate. Do not collapse this back to one wall-clock timeout; placed cubes can appear late, and verify still needs a full active nudge window before falling through to the explicit startup-hydrate fallback.
- Locked YOLO detect profile (2026-05-30, source of truth `vision_runtime.py`): duplicate/overlap behavior is tuned only with `QARM_YOLO_IOU_NMS` (default 0.5, Ultralytics NMS). The old post-dedup and mask-centroid knobs/code were removed (`QARM_YOLO_DUP_SUPPRESS_*`, `QARM_YOLO_DUP_MASK_IOU_MIN`, `QARM_YOLO_USE_MASK_CENTROID`); candidate UV is always bbox center X with the existing top-face-biased Y. Verify boot `[Tracking]` / `[DetectProfile]` logs before tuning further.
- YOLO config alignment note (2026-05-30): code defaults keep `QARM_YOLO_CONF=0.45`, `QARM_YOLO_IOU_NMS=0.5`, Bot-SORT ReID, and square 640 infer. If high-contrast face markings create duplicate boxes, tune `QARM_YOLO_IOU_NMS` rather than reintroducing post-dedup fallback logic.
- Startup anti-lag note: avoid camping on one temporarily invisible target; defer after a few visibility misses and revisit in a final pass (`QARM_STARTUP_STACK_VISIBILITY_DEFER_CHECKS`) while keeping unresolved-visible fail-loud gating.
- Startup defer note: before marking `temporarily_not_visible_deferred`, run one seed-UV recenter retry for that checklist item so the camera re-aligns to the original target spot before deferring.
- Startup tuning caution: overly aggressive low timeouts/attempt caps can collapse hydrate completeness (e.g., 6 cubes dropping to 1+1); prefer moderate lock budgets with defer+revisit rather than hard-fast fails.
- Grip safety note: persistent-load gripper warnings can occur before hard/emergency trips; use warn-level micro-relax backoff (close-time + move-time) to bleed just enough grip while preserving grasp.
- Verify-v2 tuning note: if true top placements are rejected as `placed_mismatch_out_of_margin`, prefer small verify-only margin relaxation first (XY/Z scoring) before changing tracker/centering behavior.
- Extraction note: `runtime_loop.py` currently depends on centering constants (`KYAW`, `KSHOULDER`, `KELBOW`, `CENTER_EY_KI`, `CENTER_EY_I_CLAMP`, `CENTER_EY_I_DECAY`) being available from `runtime_core` via `from runtime_core import *`; keep these names exported when modularizing centering code.
- Git workspace note: Git toplevel may resolve to `/Users/quasar/Documents/Projects` (parent of this runtime folder); scope status/diff commands to this project path when reviewing extraction changes.
- Extraction import-order note: modules imported by `runtime_core` before tunables are initialized should avoid function defaults that directly reference core constants; resolve those defaults inside function bodies after runtime-core binding to prevent import-time `NameError`.
- Extraction hygiene note: when moving code by broad line ranges, verify no dangling decorators/section fragments are left at module tail (for example, lone `@dataclass`) or py_compile will fail despite unchanged logic.
- Runtime-loop router note: do not unconditionally reassign local loop state (`centered_pos`, `cube_color`, `color_conf`) after delegated handlers run; observe/recovery handlers may update these via closure side effects, so only apply returned fields when explicitly present.
- Startup seed-retry status: seed-UV retry centering was removed from startup hydrate; on repeated invisibility, targets now defer/fail directly based on visibility/defer thresholds.
- Startup hydrate tuning update: defaults were tightened to move on faster when unstable (`QARM_STARTUP_STACK_MAX_TRACK_ATTEMPTS=2`, `QARM_STARTUP_STACK_VISIBILITY_DEFER_CHECKS=8`); override via env for slower/more persistent lock behavior.
- Startup hydrate early-exit note: when both sides reach `STARTUP_STACK_MAX_CUBES_PER_SIDE` lock-commits (default 3+3), `run_startup_stack_identity_pass` stops the checklist loop, skips refresh/defer pass 2, and does not fail on extra visible track IDs (`[StartupHydrateEarlyExit]`). Rollback: `QARM_STARTUP_STACK_EXIT_WHEN_SIDES_FULL=false`.
- Startup hydrate dedup-Z note: lock-commit `duplicate_measurement` uses `SCENE_RECON_DEDUP_Z_M * 0.5` (default 0.020 m), not full scene-recon dedup; YOLO/inverse-depth on stacked faces can under-report layer spacing (~6 cm nominal), so do not raise this back toward 0.025 m without logs showing true same-cube double-counts.
- Startup hydrate visibility note: per-frame visibility floor is `QARM_STARTUP_TARGET_MIN_CONF` (default 0.70). Detector base `QARM_YOLO_CONF` is intentionally lower (default 0.45) so Bot-SORT's low-conf cascade can bridge jittery frames; do not raise `QARM_YOLO_CONF` above 0.50 without also retuning Bot-SORT thresholds, and do not raise `QARM_STARTUP_TARGET_MIN_CONF` above the detector floor or visible cubes will defer-then-fail (watch for `[StartupHydrateRetryMiss]`).
- Startup hydrate Z-recovery note: after pass 2, if top committed Z implies more layers than committed (`STACK_LEVEL_DZ_M=0.060`), a stationary spatial-anchor pass at `PLACE_LOOKING` accepts visible cubes near `(anchor_xy, base_z + idx*DZ_M)` within `QARM_STARTUP_STACK_LAYER_MATCH_XY_M` / `QARM_STARTUP_STACK_LAYER_MATCH_Z_M` (`QARM_STARTUP_STACK_Z_PREDICT_ENABLED`, default on). When `QARM_STARTUP_STACK_REQUIRE_EXPECTED_LAYERS=true` (default), inferred expected-layer shortfalls log `[StartupHydrateExpectedShortfall]`, retry bootstrap up to `QARM_STARTUP_STACK_BOOTSTRAP_MAX_PASSES=3`, and block policy startup if still incomplete.
- Startup hydrate correction-mission note (2026-05-29): expected-layer shortfall no longer hard-stops startup when the shortfall side has partial hydration (`stack_level>0`); runtime logs one `[StartupHydrateContinue] ... reason=pick_placed_can_correct` and lets policy expose `pick_placed_<side>`. Shortfall sides with no hydrated stack still retry/fail loudly via `startup_hydrate_expected_layers_incomplete:<side>`.
- Startup hydrate inferred-color note (2026-05-30): when top Z implies a taller stack than lock commits could separate, `run_startup_stack_identity_pass` infers unresolved lower-layer colors from the directly-above measured layer after all hydrate passes finish, logs `[StartupHydrateInferredColor]`, and raises the effective section `stack_level` to `expected_stack_levels`; raw lock count is preserved in `observed_lock_stack_levels` and section `observed_lock_stack_level`. This prevents planner state from treating a physically implied occupied layer as `empty` or `unknown` when same-color occlusion is likely.
- Startup hydrate centering timer note (2026-05-30): startup calls `run_track_handoff_session(..., refresh_required_track_timeout_on_visible=True)` so the short lock/centering window refreshes while the required `track_id` is still visible (`[StartupHydrateCenterRefresh]`) instead of dropping back to track selection. The refresh remains capped by `STARTUP_STACK_LOCK_STAGE_TIMEOUT_S`.
- Startup/correction section-bound note (2026-05-29): default `QARM_MISPLACED_PICK_SECTION_MAX_DIST_M` now inherits `QARM_STARTUP_STACK_LOCK_ASSIGN_XY_MARGIN_M` so `pick_misplaced` / `pick_placed` correction accepts the same side-assignment radius that startup hydrate uses. If overriding one during tuning, override both or expect hydrate/correction boundary mismatches.
- Startup hydrate side full rescan note (2026-05-27): **disabled by default** (`QARM_STARTUP_STACK_SIDE_FULL_RESCAN_ENABLED=false`). When enabled and a side expects a full stack but checklist/defer left gaps, pass 3 runs one burst (`QARM_STARTUP_STACK_SIDE_FULL_RESCAN_FRAMES`, default 12) via `[StartupHydrateSideRescan]`. Enable only when stuck at 2/3 after defer; long rescans can starve the camera (`Frame didn't arrive within 5000`).
- Tier 1 track-stability note: track ID churn on partially-occluded base cubes is currently mitigated with `QARM_YOLO_CONF=0.45` (feeds Bot-SORT low-conf detections), `QARM_YOLO_IOU_NMS=0.5`, and `botsort_reid.yaml: track_buffer=60` (~3 s memory through transient occlusion). Do not re-add mask-centroid targeting; tune IoU/tracker thresholds instead.
- RealSense resolution note: D4xx does not support native **640x640** color+depth (`Couldn't resolve requests`). Use `QARM_REALSENSE_WIDTH=640` / `QARM_REALSENSE_HEIGHT=480` (defaults) for the camera; square **640** infer is via `QARM_YOLO_STRETCH_SQUARE` + `QARM_YOLO_STRETCH_SIZE=640`, not stream size.
- Bbox-split note: YOLO sometimes merges 2-3 vertically stacked cubes into one tall bbox. A depth-aware splitter in `vision_runtime.split_merged_stack_candidates` runs between detect and project; it triggers when bbox aspect>=1.25 and 3D delta_z>=0.050 m by current code default (`QARM_YOLO_BBOX_SPLIT_MIN_HEIGHT_M`). Off via `QARM_YOLO_BBOX_SPLIT_ENABLED=false`. Watch for `[BboxSplit]` log lines. This is separate from duplicate-box tuning and was intentionally left in place during the 2026-05-30 YOLO dedup simplification.
- YOLO NMS / startup layer-vote note (2026-05-30): locked default `QARM_YOLO_IOU_NMS=0.5` in `vision_runtime.py`; periphery stacks may need higher NMS (0.75–0.85) via env when experimenting. Pass-3 layer recovery defaults: `QARM_STARTUP_STACK_LAYER_SCAN_FRAMES=6`, `QARM_STARTUP_STACK_LAYER_VOTE_MIN_HITS=1`. Side full rescan **off** by default (`QARM_STARTUP_STACK_SIDE_FULL_RESCAN_ENABLED=false`) to avoid long extra observe bursts and camera timeouts; enable when a side is stuck at 2/3 after defer. Watch `[StartupHydrateLayerVote]` and `[StartupHydrateLayerMissing]`.
- Pass-3 measurement-anchored base note (2026-05-27): placement slot Z (used to be the base anchor) sits ~0.04 m below the actual projected cube Z because YOLO/projection reads the visible front/top face of a stacked cube, not its centroid. Pass 3 derives `stack_base_z` from the LOWEST committed cube Z. Layer-match defaults: `QARM_STARTUP_STACK_LAYER_MATCH_XY_M=0.030`, `_Z_M=0.025`. Diagnostic `[StartupHydrateLayerAnchor]` / `[StartupHydrateLayerNearMiss]`. Widen gates only when logs show near-misses, not invisible cubes.
- Grip-current tuning note (2026-05-27, rev 2): defaults tightened to avoid firmware persistent-load trips, then partially relaxed after a follow-up dropped-cube during lift. Close-time defaults: `QARM_GRIP_WARN_A=0.58`, `QARM_GRIP_HARD_A=0.61`, `QARM_GRIP_EMERGENCY_A=0.64`, `QARM_GRIP_MIN_OVERCURRENT_CMD=0.30` (overcurrent gate engages early in the ramp - diagnostic visibility), `QARM_GRIP_WARN_RELAX_DEBOUNCE=3`. Lift-time motion supervisor: `QARM_MOVE_RELAX_STEP=0.0030` (halved from 0.0060). The "overcurrent sensed while gated" log line prints `i_last`, `peak`, warn/hard/emergency thresholds, and live grip_cmd. Rollback via env vars; do not raise emergency back near firmware persistent-load trip (~0.67 A) without revalidation.
- Pick-workspace Y gate note (2026-05-27): table/feed picks (`observe_scene`, `grasp_cube`, `pick_other`) share `xyz_in_pick_workspace` / `pick_workspace_y_max_m()` (`PICK_MAX_BASE_Y_M + SCENE_RECON_PLACE_Y_MARGIN_M`, override `QARM_PICK_WORKSPACE_Y_MAX_M`). `pick_other` handoff sets `enforce_pick_workspace_only=True` so left/right stack cubes (high Y) are skipped with `candidate_pick_space_rejected` (structural skip, no reject-count burn). `pick_placed_*` uses the opposite gate (`y <= correction_pick_min_y_m` rejects table picks) — do not enable pick-workspace on misplaced correction.
- Pick-placed prehandoff pick-space gate note (2026-05-30): `QARM_MISPLACED_PICK_ENFORCE_PICK_SPACE_GATE` now defaults true and passes `reject_below_base_y_m=0.0` into `run_track_handoff_session` for `pick_placed_*` / correction handoff. This skips negative-Y pick-space tracks before centering (`reason=negative_y_pick_area`) while the post-lock median safety reject remains as backup.
- Future recovery note (2026-05-30): if a `pick_misplaced` / `pick_placed` correction knocks a cube down or leaves it outside the section-center radius, a fallen-cube recovery pass or explicit startup-hydrate recovery sequence may be useful. Do not implement this yet; prioritize fixing/tuning `pick_misplaced` targeting first because a displaced cube can otherwise consume all correction attempts and prevent run completion.
- Grasp pick X bias (2026-05-27): default `QARM_GRASP_PICK_X_BIAS_M=0.010` (+X forward). Was `0.050` and caused ~56 mm overshoot toward the right column when stacks are ~54 mm apart in X; rollback wider bias only for table/loose picks via env.
- Grasp stack-forward X note: default **off** (`QARM_GRASP_STACK_FORWARD_ENABLE=false`, `per_level=0`). When enabled, measured Z above `QARM_GRASP_STACK_FORWARD_Z_START_M` adds `floor((z-z_start)/STACK_LEVEL_DZ_M) * per_level` +X on pick. Boot `[GraspTune] stack_fwd=...` and grasp log `stack_fwd=+...`.
- Stack-forward default caution (2026-05-29): `runtime_core.py` currently defaults `QARM_GRASP_STACK_FORWARD_ENABLE` to **true** despite older notes saying default off; when this flag is true, `pick_misplaced` correction grasps also use the 40% grip-step boost logged as `[PickMisplacedGripStep]` (`mult=1.40`).
- Stack-forward cap caution (2026-05-29; corrected 2026-05-30): `QARM_GRASP_STACK_FORWARD_PER_LEVEL_M` is intended to be a small per-level nudge and currently defaults to `0.006` m (6 mm), capped by `QARM_GRASP_STACK_FORWARD_MAX_M=0.016`. Do not restore the accidental `0.060` m default.
- Grasp pick Y bias (2026-05-27): default `QARM_GRASP_PICK_Y_BIAS_M=0.006` (+Y toward higher-Y / left stacks in base frame). Was `0.002`. Rollback: `0.002` or `0.000`.
- Grasp Z depth (2026-05-27): `depth = min((1-frac)*edge + QARM_GRASP_Z_DEPTH_FROM_TOP_M, 0.5*edge)`, `z_pick = max(z_meas - depth, TABLE_Z_SAT)`. Default `QARM_GRASP_Z_PICK_FRACTION=0.50` (cube **midpoint**, ~30 mm on 60 mm edge). Optional flat add default `0` (`QARM_GRASP_Z_DEPTH_FROM_TOP_M`). Rollback shallower: `frac=0.72`; optional flat add only if needed: `QARM_GRASP_Z_DEPTH_FROM_TOP_M=0.020`.
- Far-XY grasp Z lift (2026-05-29): temporarily disabled by default (`QARM_GRASP_FAR_XY_Z_LIFT_ENABLED=false`, `QARM_GRASP_FAR_XY_Z_LIFT_M=0.0000`) because it only changes pick Z, not place Z. Re-enable only for pick-space A/B testing; boot/log line includes `far_xy_lift`.
- Stack place XY/Z anchor (2026-05-29): level-0 `place_left` commits `[StackAnchorCommit]` XY (slot + `PLACE_Y_BIAS_M`). `place_left_stack` levels 1-2 reuse that XY **without** adding bias again (`[StackAnchor] ... bias_skipped=yes`). Runtime stack geometry uses `QARM_STACK_LEVEL_DZ_M=0.060`; place-only upper-layer Z now also uses `QARM_PLACE_STACK_LEVEL_DZ_M=0.060` (was 0.055), plus `QARM_PLACE_STACK_UPPER_EXTRA_Z_M=0.003` on zero-based stack levels 1-2 and `QARM_PLACE_STACK_LEVEL3_EXTRA_Z_M=0.005` on zero-based stack level 2 / the third cube. With `QARM_PLACE_RELEASE_Z_M=0.037`, stack release Z targets are level1=0.100 and level2=0.165.
- Stack anchor bias log caution: `[StackAnchor] ... bias_skipped=yes` refers to slot/place XY bias only. As of 2026-05-29, stack-mode skips `[PlacePickBiasComp]` when XY is locked to a stack anchor (`QARM_PLACE_PICK_BIAS_COMPENSATE_STACK_ANCHOR_ENABLED=false`, default) so level-1/2 commands do not move off the measured/commanded anchor a second time. Re-enable only for A/B testing.
- Place release Z note (2026-05-29): default `QARM_PLACE_RELEASE_Z_M=0.037` for now. Keep changes small (1-3 mm steps); this also raises stack release targets because stack Z starts from slot Z.
- Return cube Z note (2026-05-29): normal `return_cube` adds `QARM_RETURN_CUBE_Z_LIFT_M=0.005` above `max(pick_return_z, PLACE_RELEASE_Z_M)` before safe_place; log `[ReturnCubeZ]`. This is separate from stack place and `pick_misplaced` return-grid Z.
- Grasp pre-lift bleed (2026-05-27): after close, before lift, `GRASP_PRE_LIFT_BLEED_ENABLED` relaxes grip toward carry band when `hold_grip >= GRASP_PRE_LIFT_BLEED_GRIP_TRIGGER` (0.55) or close peak `>= GRASP_PRE_LIFT_BLEED_PEAK_A` (0.52 A); log `[GraspPreLiftBleed]`. Lift slowed: `QARM_GRASP_LIFT_SEGMENT_S=1.80`, `QARM_GRASP_LIFT_STEPS=90`, `QARM_GRASP_SETTLE_BEFORE_LIFT_S=0.20`. Lift fail prints joint diag. After `lift_failed`, home uses open grip (`[PickFailOpen]`). Rollback: `QARM_GRASP_PRE_LIFT_BLEED_ENABLED=false`, `QARM_GRASP_LIFT_SEGMENT_S=1.0`.
- Carry-grip / close ramp (2026-05-27, rev 5; grip cap retuned 2026-05-28; close step +5% on 2026-05-30): `QARM_MAX_GRIP_CMD` / `QARM_GRIP_DEFAULT` default **0.58**. Slower close/tune/bleed steps to reduce slip: `QARM_GRIP_STEP=0.004725`, `QARM_GRIP_RELAX_STEP=0.008`, `QARM_GRIP_WARN_RELAX_STEP=0.006`, `QARM_GRIP_MAX_CLOSE_S=2.0`, `QARM_POST_LIFT_TUNE_STEP=0.0025`, `QARM_POST_LIFT_CARRY_TUNE_STEP=0.002`, `QARM_GRASP_PRE_LIFT_BLEED_TUNE_STEP=0.002`, `QARM_GRASP_PRE_LIFT_BLEED_MAX_STEPS=4`. Post-lift carry bleed `grip_cmd` trigger defaults to **max grip** (0.58), not 0.55. Move-time bleed unchanged (`Ig > 0.28 A`). Rollback faster close: `QARM_GRIP_STEP=0.0075`, `QARM_POST_LIFT_CARRY_GRIP_TRIGGER_CMD=0.55`; rollback cap via `QARM_MAX_GRIP_CMD=0.59`, `QARM_GRIP_DEFAULT=0.59`.
- Place release note (2026-05-28): default place release now fully opens at the stack pose and maintains full-open during clearance (`QARM_PLACE_RELEASE_TOUCH_OPEN_GRIP=0.24`, same as `QARM_PLACE_RELEASE_OPEN_GRIP=0.24`), with slower release duration `QARM_PLACE_RELEASE_S=1.5`. `place_release_clear` should keep `release_open_grip`; do not reintroduce partial-open lift unless testing shows the cube shifts during full-open.
- Carry-bleed accept policy (2026-05-28): post-lift carry bleed max steps default is `QARM_POST_LIFT_CARRY_MAX_STEPS=6` (was 12). Do not accept a post-lift bleed if reverify fails below the normal carry hit requirement; the old `partial_accept_overcurrent` path could over-relax after pre-lift + move-time bleed and drop median below `QARM_POST_LIFT_MIN_A`, causing immediate pick verify failure.
- Post-lift verify current threshold (2026-05-29): default `QARM_POST_LIFT_MIN_A=0.112` (was 0.120) to avoid false dropped-cube failures on borderline but stable carries. This is the `[PostLiftVerify] ... above ... A` threshold and also feeds `QARM_PICK_STABILITY_RECHECK_MIN_A` unless overridden.
- Correction retreat/home (2026-05-27): after successful `pick_placed_*` / `pick_misplaced_*` return-grid drop, `retreat_after_correction_drop` goes straight to `HOME` (grip 0); log `[CorrectionRetreat] home_ok=...`. Default on: `QARM_CORRECTION_RETREAT_HOME_ENABLED=true`. Optional pre-drop transit: `QARM_CORRECTION_DROP_TRANSIT_ENABLED=true` uses `CORRECTION_DROP_TRANSIT` before `safe_place`; log `[CorrectionDropTransit]`.
- Correction grasp offset note (2026-05-29; tuned 2026-05-30): `pick_placed_*` / `pick_misplaced_*` grasps add correction-only command offsets after the normal grasp bias/stack-forward calculation. Current correction X offset is disabled (`QARM_PICK_MISPLACED_GRASP_X_OFFSET_M=0.000`; previous `-0.006` removed). Base correction Z remains `QARM_PICK_MISPLACED_GRASP_Z_OFFSET_M=+0.006`, plus height-aware `+Z` via `QARM_PICK_MISPLACED_GRASP_HIGH_Z_EXTRA_M=+0.010` when measured selected level > 0 to avoid pushing down on stacked cubes; height-aware `+Y` uses `QARM_PICK_MISPLACED_GRASP_Y_PER_LEVEL_M=+0.005` per measured stack level (base `QARM_PICK_MISPLACED_GRASP_Y_OFFSET_M=0`, cap `QARM_PICK_MISPLACED_GRASP_Y_MAX_M=+0.010`); log `[PickMisplacedGraspOffset]`. Normal table picks are unchanged.
- Correction failure refresh note (2026-05-29; changed 2026-05-30): after failed `pick_placed_*` / `pick_misplaced_*` while not holding, startup hydrate refresh is gated by `QARM_PICK_CORRECTION_FAIL_HYDRATE_REFRESH_ENABLED` (default false). When enabled it logs `[PickCorrectionFailRefresh] ... hydrate_begin mode=refresh` and syncs stack levels; when disabled, the same summary log reports `refresh_status=skipped_disabled`.
- Pick-misplaced return grid note (2026-05-29): return/drop base X defaults to the tower slot X (`PLACE_GRID_CENTER_X_M + PLACE_GRID_DX_M`, currently 0.480) for a more balanced drop, then slots step with `QARM_MISPLACED_RETURN_GRID_DX_M=-0.115` and `_DY_M=-0.125`; return Z defaults to `max(TABLE_Z_SAT_M, PLACE_RELEASE_Z_M) + QARM_MISPLACED_RETURN_DROP_Z_LIFT_M` (`+0.010` m default). Override base X with `QARM_MISPLACED_RETURN_DROP_X_M` or absolute Z with `QARM_MISPLACED_RETURN_DROP_Z_M`.
- Observe empty-confirmation note (2026-05-29): `active_detection_timeout` after centering saw/selectable candidates is not an empty scene. It logs `[ObserveMiss] cause=active_detection_timeout ... empty_count_unchanged=...` and does not increment `no_pick_miss_count`; `runtime_loop_observe` also keeps `observe_fail_streak` unchanged and logs `[ObserveSafety] miss_streak_unchanged=... reason=visible_target_active_timeout` so visible cubes do not force `stop_run_only`. If this follows recent grasp failure(s), `[PickSpaceOrientationCheck]` is advisory (`action=warn_continue`) and returns normal `retry` instead of forcing `recurrent_grasp_failures_check_pick_space_orientation`; the planner can still choose `stop_run` later if it keeps looping. True `no_detection_timeout` still uses the normal empty confirmation path.
- Pick-other/observe timing note (2026-05-29): generic observe centering already uses split 5s no-detection + 5s active-target windows (`CENTER_TIMEOUT_NO_DETECTION_S`, `CENTER_TIMEOUT_ACTIVE_S`). Track-handoff sessions used by `pick_other` now default `QARM_TRACK_HANDOFF_NO_CANDIDATE_TIMEOUT_S=5.0` (was 3.0) and reset the no-candidate deadline when candidates reappear, so observe/pick_other failures should mostly reflect centering/grasp behavior rather than short search windows. `pick_other` logs `hard_timeout=... no_candidate_timeout=...`.
- Pick target lock caution (2026-05-29): the generic observe/pick sticky-track experiment (`[TrackLock]`, `[TrackLockReacquire]`, and removing the pre-centering track-memory prime) was reverted because low-framerate track churn plus pick filtering caused observe to wait on stale/non-eligible tracks. Generic observe/pick is back to the older per-frame top/closest selection behavior with one `observe_scene_frame` track-memory prime before `center_object_slowly`. Do not reintroduce sticky observe locks without a stronger physical-cube association plan.
- YOLO hot-path note (2026-05-29): `YOLODetector.detect_candidates_and_draw(draw=False)` avoids copying the frame, and headless centering skips OpenCV overlay drawing unless a window or localization capture is enabled. Generic observe/pick intentionally keeps the pre-centering track-memory prime after the sticky-lock rollback.
- Pick-placed sole-track retry (2026-05-27): if handoff blocks the only visible target-side track, `[PickMisplacedRetry] sole_visible_track` clears `failed_track_ids` (cap `QARM_PICK_PLACED_SOLE_TRACK_RETRIES=2`) instead of burning remaining lock attempts with no candidate.
- Pick-placed lock timeouts (2026-05-27): `pick_placed_left/right` use longer handoff budgets than generic `pick_misplaced`: per-attempt `QARM_PICK_PLACED_LOCK_TIMEOUT_S=10` (vs `QARM_MISPLACED_PICK_HARD_TIMEOUT_S=6`), total `QARM_PICK_PLACED_MAX_TOTAL_LOCK_TIME_S=30` (vs `18`). Boot `[PickMisplacedHeightCfg] pick_placed_lock_timeout_s=...`. Does not fix wrong handoff rank; helps slow centering on the right track.
- Centering defaults (2026-05-29): `QARM_CENTERED_FRAMES_REQUIRED=3` (was 8) and `QARM_CENTER_PX_TOL=7` (was 5, originally 8). Affects all generic observe/pick locks where `|ex|,|ey| <= PX_TOL`; startup/verify may pass their own frame counts. `QARM_CENTER_EY_KI=0.00010` (was 0.00008). Integral **range gate** (`QARM_CENTER_EY_I_RANGE_GATE_ENABLED`, default **false**): when true, ey integral accumulates only while `|ey| <= QARM_CENTER_EY_I_ENABLE_ABS_PX` (25 px) and leaks outside that band; when false, integral accumulates at all |ey| (still clamped/decayed). Boot: `[CenterCtrl] px_tol=7 ... i_range_gate=...`. Rollback tighter centering: `QARM_CENTER_PX_TOL=5`, `QARM_CENTERED_FRAMES_REQUIRED=8`.
- Pick-other / planner color note (2026-05-28): after `pick_other` locks a new target, planner `pick_target.color` and grasp phase now use `pregrasp_pick_lock_*` when live `cube_color` was reset to unknown (avoids classify→pick_other loops). `pick_other` rejects handoff accepts with the same color as the classify seed when `QARM_PICK_OTHER_REJECT_SAME_COLOR=true` (default). Rollback: `QARM_PICK_OTHER_REJECT_SAME_COLOR=false`.
- Pick-lock state reset note (2026-05-28): after any completed place, clear `pregrasp_pick_lock_*`, `active_target_track_id`, local `centered_pos`, `cube_color`, `color_conf`, hold grip, and carry supervisor. Plain `observe_scene` must begin by clearing the old lock and must save `observe_lock` with color `unknown`; otherwise stale classified color can make the next target skip `classify_cube` and can leak into `held_cube`.
- Offline smoke-test usage note (2026-05-30): do not spend tokens/time running or editing `tests.offline_smoke_tests` by default. Ignore the offline smoke suite unless the user explicitly asks for it; prefer targeted syntax/import checks for small prompt or tuning changes.
- Place-current recovery note (2026-05-28): `move_overcurrent_unrecoverable` during a place while `state.holding_object=True` is treated as a collision recovery path in `runtime_loop_actions_place.py`: open the gripper in place with `PLACE_RELEASE_OPEN_GRIP`, clear held/centered/pick-lock state, run startup bootstrap hydrate in `mode="refresh"`, sync stack levels, and continue the next policy cycle. Do not move HOME before the release; if the recovery itself fails, then fail loudly with `place_collision_recovery_failed:*`.
- Verify-v2 surface-Z note (2026-05-28): placement ledger `expected_xyz` remains the commanded release/contact point, but verify-v2 scores against `expected_eval` with `QARM_PLACE_VERIFY_V2_SURFACE_Z_OFFSET_M=0.030` by default so top-face depth measurements around z ~= 0.058-0.065 pass for a base release z of 0.035. Keep this verify-only; tune physical release z separately from camera top-surface readings.
- Mission prompt note (2026-05-28): prompt trial mission text was restored to shorter proven wording in both `runtime_core.py` defaults and `experiments/prompts.json`. Keep Prompt 1 simple (`Stack Blue cubes left`, `Stack Orange cubes right`), Prompt 2 orange-right with left empty, and Prompt 3 alternating [BLUE, ORANGE, BLUE]. Use side-specific `pick_placed_left/right` wording rather than generic `pick_placed_cube`, because the current policy validator rejects generic correction commands.
- Test package note (2026-05-28): keep `tests/__init__.py`. Without it, `python -m unittest tests.offline_smoke_tests...` can resolve `tests` to an installed site-packages package and fail with `ModuleNotFoundError: No module named 'tests.offline_smoke_tests'`; path-based invocation still works but hides the packaging issue.
- Pick-misplaced log / lift carry note (2026-05-27, rev 4): `[PickMisplaced] failed` is one compact line via `_compact_misplaced_summary` in `runtime_loop_actions_correction.py` (side, xyz, lock hits/attempts/exit, drop_grid ok/reason, return flags). Do not grep removed nested `misplaced_lock` blobs on failure lines; use upstream `[PickMisplacedTop2]` / `[Pick]` logs for depth. Lift carry tuning iterated: rev 3 used HARD=3.5/EMERG=3.7 but a 3.599 A lift spike still tripped `hard_threshold_debounce`, froze the trajectory, and the frozen joints kept Itotal>total_warn (2.9), so the supervisor never recovered before `freeze_timeout_s=2.0` and aborted to unrecoverable. Rev 4 raises the motion total thresholds to absorb the observed lift profile: `QARM_MOVE_TOTAL_HARD_A=3.7`, `QARM_MOVE_TOTAL_EMERGENCY_A=3.85`, `QARM_MOVE_DEBOUNCE=8` (longer debounce so short transients don't compound to a freeze). `MotionGripSupervisor` freeze still only bleeds grip when gripper current >= `gripper_warn_a`; joint-only spikes log `[CurrentGuard] event=freeze_hold_grip` and keep grip. Do not raise motion total emergency above ~4.0 A without revalidation - real collisions still need to trip. Rollback: `QARM_MOVE_TOTAL_HARD_A=3.5`, `QARM_MOVE_TOTAL_EMERGENCY_A=3.7`, `QARM_MOVE_DEBOUNCE=6`.
